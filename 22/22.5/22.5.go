package main

//利用二次互反律计算勒让德符号

func main() {
	// a, err := Jacobi(5, 3593) // -1
	// a, err := Jacobi(5, 3889) // 1
	// a, err := Jacobi(14, 137) // 1
	// a, err := Jacobi(55, 179) // -1
	// a, err := Jacobi(299, 397) // -1
	// a, err := Jacobi(37603, 48611) // 1
	// a, err := Jacobi(85, 101) // 1
	// a, err := Jacobi(29, 541) // -1
	// a, err := Jacobi(101, 1987) // 1
	// a, err := Jacobi(31706, 43789) // -1

	// fmt.Print(a, err)
}
